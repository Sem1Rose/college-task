#include <iostream>

using namespace std;
int main() {
  int A = 5, B = 8;
  if (A > B)
    cout << "A is greater than B" << endl;
  else
    cout << "B is greater than or equal to A" << endl;

  return 0;
}